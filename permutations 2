https://leetcode.com/problems/permutations-ii/submissions/1952628486/

class Solution {
    public List<List<Integer>> permuteUnique(int[] nums) {
        Arrays.sort(nums);
        boolean used[] = new boolean[nums.length+1];
        Set<List<Integer>> ans = new HashSet<>();
        Backtrack(nums,new ArrayList<>(),ans,used);
        return new ArrayList<>(ans);
    }

    public static void Backtrack(int nums[],ArrayList<Integer> temp,Set<List<Integer>> ans,boolean used[]){
        if(temp.size()==nums.length){
            ans.add(new ArrayList<>(temp));
            return;
        }
        for(int i=0;i<nums.length;i++){
            if(used[i]) continue;

            temp.add(nums[i]);
            used[i] = true;

            Backtrack(nums,temp,ans,used);

            temp.remove(temp.size()-1);
            used[i] = false;
        }
    }
}
